# release.yml is generated by `dist generate` (cargo-dist) and must not be
# hand-edited, so its shellcheck style/info findings are ignored here.
paths:
  .github/workflows/release.yml:
    ignore:
      - 'shellcheck reported issue in this script: SC2086:info'
      - 'shellcheck reported issue in this script: SC2129:style'
